# LegalLens AI v2 — requirements.txt

# ── Core framework ──────────────────────────
streamlit>=1.32.0

# ── AI / ML ─────────────────────────────────
transformers>=4.38.0,<4.50.0
peft>=0.10.0                    # LoRA model support (optional)
torch>=2.1.0
sentence-transformers>=2.6.0      # Embeddings for RAG
anthropic>=0.25.0                  # Claude API for RAG chat (optional)

# ── Vector store ────────────────────────────
faiss-cpu>=1.7.4                   # FAISS for semantic search

# ── LangChain (chunking) ─────────────────────
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1

# ── PDF / OCR ───────────────────────────────
pdfplumber>=0.10.0
pytesseract>=0.3.10
pdf2image>=1.17.0
opencv-python-headless>=4.9.0
Pillow>=10.0.0

# ── PDF Report export ────────────────────────
reportlab>=4.1.0

# ── NLP utilities ────────────────────────────
nltk>=3.8.1                        # Sentence tokenization
sentencepiece>=0.1.99
tiktoken>=0.6.0

# ── Misc ─────────────────────────────────────
numpy>=1.24.0
python-dotenv>=1.0.0
